# Set to "false" once the backend is up and running to switch the app
# from the built-in mock data to the real API.
VITE_USE_MOCKS=true

# Only used once VITE_USE_MOCKS=false.
VITE_API_URL=http://localhost:4000/api
VITE_WS_URL=http://localhost:4000
